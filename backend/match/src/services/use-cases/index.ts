import findMatch from "./find-match";
import cancelMatch from "./cancel-match";

const matchUseCases = Object.freeze({
  findMatch,
  cancelMatch,
});

export default matchUseCases;

export { findMatch, cancelMatch };
