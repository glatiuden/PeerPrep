apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: user
  labels:
    app: user
  namespace: peerprep
spec:
  maxReplicas: 3
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user
  targetCPUUtilizationPercentage: 50
